# WeatherJsonDataParser

Util that takes weather data from api.openweathermap.org,
parsing json into POJO with required fields (ExternalData.class).
Can parse our POJO to json for sending it anywhere else
